# Required: your Anthropic API key
ANTHROPIC_API_KEY=sk-ant-...

# Where photos and the SQLite database are written
DATA_DIR=./data

# Only needed to run the evaluation suite: the folder holding the
# hand-written sheet photos and their ground-truth JSON (kept outside this repo)
EVAL_DATASET_DIR=../taller/dataset
